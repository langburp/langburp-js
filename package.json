{
  "name": "langburp-js",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate": "cd packages/langburp-js && npm run generate && npm run build"
  },
  "devDependencies": {
    "@changesets/cli": "2.27.12",
    "prettier": "^3.4.2",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "examples/*",
    "packages/*"
  ]
}